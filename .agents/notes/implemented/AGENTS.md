# implemented/ — Agent Notes

Implemented Agent Notes record decisions that shipped and are kept current with what actually shipped.
Moving a note into implemented/ (from proposed/) rewrites its Proposal into a present-tense Decision,
folds acceptance criteria and risks into Consequences, and drops plan-only sections. When later code
moves a file, renames a package, or changes a key/default, update this note's facts in the same change.
