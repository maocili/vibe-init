interface:
  display_name: "Pre-Push Checks"
  short_description: "Resolve scope, run declared checks, commit, and verify the push"
  default_prompt: "Use $pre-push-checks to resolve the base, run declared checks, commit, and verify this push."
