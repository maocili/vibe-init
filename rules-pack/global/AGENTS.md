<!-- dsh-rules:global-standing-orders -->
<!-- 待填充 [实现期]：从 vibe-coding-templates 提炼「全局常设规则」全文。
     物化目标：~/.dsh/AGENTS.md（agent-instructions USER_GLOBAL_FILE，随每次会话基线注入）。
     注意字节预算（DESIGN §10-3）：agent-instructions 对 USER_GLOBAL_FILE 有 maxBytes 上限，
     需在「一句话指针」与「内联完整规则」间取舍。

     规划内容段（提炼自 vibe 纪律）：
       1. 何时写 Agent Note / 决策记录（触发点清单）
       2. Agent Note 格式与命名指针 → 指向各项目 .agents/notes/ 骨架约定
       3. 文本链接管理（开 feature 时追加，见 features/text-link-management.md）
       4. 双语文档纪律（开 feature 时追加，见 features/bilingual-docs.md）
       5. 文档分层 / slop 清单
-->

（占位：内容待填充）
