interface:
  display_name: "Pre-Push Checks"
  short_description: "Inspect scope, run focused checks, and verify the push"
  default_prompt: "Use $pre-push-checks to inspect, validate, commit, and push this change."
